<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/Blanco" /> <!-- Cambia el color de fondo -->
    <stroke
        android:width="2dp"
        android:color="@color/RojoOb" /> <!-- Cambia el color del borde -->
    <corners
        android:radius="2dp"/>
</shape>
