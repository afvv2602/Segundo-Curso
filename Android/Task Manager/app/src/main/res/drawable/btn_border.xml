<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/AmarilloCl" /> <!-- Cambia el color de fondo -->
    <stroke
        android:width="2dp"
        android:color="@color/Amarillo" /> <!-- Cambia el color del borde -->
    <corners
        android:radius="5dp"/>
</shape>
