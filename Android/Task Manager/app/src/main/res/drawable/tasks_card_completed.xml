<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/VerdeCl" /> <!-- Cambia el color de fondo -->
    <stroke
        android:width="2dp"
        android:color="@color/VerdeOb" /> <!-- Cambia el color del borde -->
    <corners
        android:radius="5dp"/>
</shape>
